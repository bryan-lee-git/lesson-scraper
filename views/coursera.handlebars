<main>
    <div class="container center-align">
        <div class="row">
            <br />
            <br />
            <br />
            <img class="col s12 m4 offset-m4" src="/img/coursera-swiper.png" alt="">
        </div>
        <div class="col s12 m6">
            <table class="responsive-table centered highlight z-depth-1 blue lighten-5" id="coursera-table">
                <thead>
                    <th>Title (Click to Navigate to Lesson)</th>
                    <th>Description</th>
                    <th>Notes</th>
                </thead>
                <tbody>
                    {{#each coursera}}
                        <tr>
                            <td><a href="{{link}}">{{title}}</a></td>
                            <td>{{description}}</a></td>
                            <td><button class="btn">Notes</button></td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</main>