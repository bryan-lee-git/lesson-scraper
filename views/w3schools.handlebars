<main>
    <div class="container center-align">
        <div class="row">
            <br />
            <br />
            <br />
            <img class="col s12 m4 offset-m4" src="/img/w3-swiper.png" alt="">
        </div>
        <div class="col s12 m6">
            <table class="responsive-table centered highlight z-depth-1 blue lighten-5" id="w3-table">
                <thead>
                    <th>Section</th>
                    <th>Lesson (Click to Navigate to Lesson)</th>
                    <th>Notes</th>
                </thead>
                <tbody>
                    {{#each w3}}
                        <tr>
                            <td>{{section}}</td>
                            <td><a href="{{link}}">{{title}}</a></td>
                            <td><button class="btn">Notes</button></td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</main>